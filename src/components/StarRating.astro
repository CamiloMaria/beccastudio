---
interface Props {
  rating: number;
}

const { rating } = Astro.props;
---

<div class="flex gap-1">
  {
    [...Array(5)].map((_, index) => (
      <span class={index < rating ? "text-gold-light" : "text-gray-400"}>
        ★
      </span>
    ))
  }
</div>
